 - name: BT_APP
   ver: '1.4.x'
   scope: BT_APP
   lang: javascript
   groups:

   - name: UI组件
     lang: javascript
     snippets:

     - name: bui-accordion
       displayName:  折叠菜单初始化
       display: false
       desc: 
       code: |
         var uiAccordion = bui.accordion({
          id:"#uiAccordion"
         });

     - name: bui-accordion-demo
       displayName:  折叠菜示例
       display: true
       desc: 
       code: |
        // 折叠菜单 js 初始化:
          var uiAccordion = bui.accordion({
              id:"#uiAccordion"
          });

        // 折叠菜单 html 对应的结构:
        <dl id="uiAccordion" class="bui-accordion">
          <dt class="bui-btn bui-box">
            <div class="span1">折叠菜单</div>
            <i class="icon-accordion"></i>
          </dt>
          <dd>
           折叠内容
          </dd>
        </dl>

     - name: bui-actionsheet
       displayName:  上拉菜单初始化
       display: false
       desc: 
       code: |
        var uiActionsheet = bui.actionsheet({
            trigger: "#${1:uiActionsheet}",
            buttons: [{ name:"分享到微博",value:"weibo" }],
            callback: function (e,ui) {
                var val = \$(this).attr("value");
                if( val == "cancel"){
                    ui.hide();
                }
            }
        })

     - name: bui-actionsheet-demo
       displayName: 上拉菜示例
       display: true
       desc: 
       code: |
        // 上拉菜单 js 初始化:
        var uiActionsheet = bui.actionsheet({
          trigger: "#btnOpen",
          buttons: [{ name:"分享到微博",value:"weibo" },{ name:"分享到微信",value:"weixin" },{ name:"分享到QQ",value:"QQ" }],
          callback: function (e,ui) {

            var val = $(this).attr("value");

            if( val == "cancel"){
              ui.hide();
            }
          }
        })

        // 上拉菜单 html 对应的结构:
        <div id="btnOpen" class="bui-btn">打开下拉菜单</div>

     - name: bui-btn-load
       displayName:  绑定按钮跳转
       display: false
       desc: 
       code: |
        bui.btn({id:"${1:#page}",handle:"${2:.bui-btn}"}).load();

     - name: bui-btn-submit
       displayName:  绑定按钮提交
       display: false
       desc: 
       code: |
        bui.btn("${1:#btn}").submit(function(loading){
          ${2:loading.stop();}
        });

     - name: bui-alert
       displayName:  对话框
       display: false
       desc: 
       code: |
        bui.alert("${1:提醒内容}");

     - name: bui-confirm
       displayName:  确认框
       display: false
       desc: 
       code: |
        bui.confirm("${1:提醒内容}",function(ui){
            // this 为底部按钮
            var text = \$(this).text();
            if( text == "确定" ){
              //do something
            }
          ${2:ui.close();}
        });

     - name: bui-dialog
       displayName:  自定义对话框初始化
       display: false
       desc: 
       code: |
        var uiDialog = bui.dialog({
            id: "#${1:uiDialog}"
        });

     - name: bui-dialog-demo
       displayName:  自定义对话示例
       display: true
       desc: 
       code: |
        // 弹出框 js 初始化:
        var uiDialog = bui.dialog({
            id: "#uiDialog"
        });
        // 弹出框 html 对应的结构:
        <div id="uiDialog" class="bui-dialog" style="display:none;">
          <div class="bui-dialog-head">标题</div>
          <div class="bui-dialog-main">提示内容</div>
          <div class="bui-dialog-foot">底部</div>
          <div class="bui-dialog-close">
            <i class="icon-close"></i>
          </div>
        </div>

     - name: bui-hint
       displayName:  自动消失提醒
       display: false
       desc: 
       code: |
        bui.hint("${1:提醒内容}");

     - name: bui-dropdown
       displayName:  下拉菜单初始化
       display: false
       desc: 
       code: |
        var uiDropdown = bui.dropdown({
            id: "#${1:uiDropdown}"
        });

     - name: bui-dropdown-demo
       displayName:  下拉菜单初始示例
       display: true
       desc: 
       code: |
        // 下拉菜单 js 初始化:
        var uiDropdown = bui.dropdown({
            id: "#uiDropdown"
        })
        // 下拉菜单 html 对应的结构:
        <div id="uiDropdown" class="bui-dropdown">
          <div class="bui-btn bui-box" >
            <div class="span1">下拉菜单</div>
            <i class="icon-dropdown"></i>
          </div>
          <ul class="bui-list">
            <li class="bui-btn" value="11">
              下拉的选项
            </li>
          </ul>
        </div>

     - name: bui-list
       displayName:  下拉刷新加载列表初始化
       display: false
       desc: 
       code: |
        var uiList = bui.list({
            id: "#${1:uiScroll}",
            url: "${2:userlist.json}",
            data: {},
            template: template,
            onLoad: function (scroll) {
                // 自定义渲染
            },
            callback: function (e) {
                // 点击单行回调 console.log(\$(this).text())
            },
            height: 0,
            page:1,
            pageSize:10,
            field: {
                page: "page",        // 分页字段
                size: "pageSize",    // 页数字段
                data: "data"         // 数据
            }
        });
        //生成列表的模板
        function template (data) {
            var html = "";
                \$.each(data,function(index, el) {
                    html += '<li class="bui-btn"><i class="icon-facefill"></i>'+el.name+'</li>';
                });
            return html;
        };

     - name: bui-list-demo
       displayName:  下拉刷新加载列示例
       display: true
       desc: 
       code: |
        // 列表控件 js 初始化:
        var uiList = bui.list({
            id: "#uiList",
            url: "",
            data: {},             // 数据请求带过去的参数,分页在field配置
            template: listTemplate,
            field: {
                page: "page",     // 分页参数名
                size: "pageSize", // 分页大小参数名
                data: ""          // 数据字段名
            }
        });
        // 列表模板
        function listTemplate (data) {
            var html = "";
            \$.each(data,function(index, el) {
                html += '<li class="bui-btn" href="index.html"><i class="icon-facefill"></i>'+el.name+'</li>';
            });

            return html;
        }
        // 列表控件 html 对应的结构:
        <div id="uiList" class="bui-scroll">
            <div class="bui-scroll-head"></div>
            <div class="bui-scroll-main">
                <ul class="bui-list">
                </ul>
            </div>
            <div class="bui-scroll-foot"></div>
        </div>

     - name: bui-listview
       displayName:  侧滑列表初始化
       display: false
       desc: 
       code: |
        var uiListview = bui.listview({
            id: "#${1:uiListview}",
            data: [{ "text": "删除", "classname":"danger"}],
            callback: function (e,ui) {
                // this 滑动出来的操作按钮
                var text = \$(this).text();
                if( text == "删除" ){
                    //do something
                }
                ${2:ui.close();}
            }
        });

     - name: bui-listview-demo
       displayName:  侧滑列示例
       display: true
       desc: 
       code: |
        // 侧滑列表 js 初始化:
        var uiListview = bui.listview({
            id: "#uiListview",
            data: [{
                "text": "修改",
                "classname":"primary"
            },{ "text": "删除",
                "classname":"danger"
            }],
            callback: function (e,menu) {
                var text = \$(this).text().trim();
                if( text == "删除"){
                    // 删除
                }
                //关闭菜单
                menu.close();
            }
        });

        // 侧滑列表 html 对应的结构:
        <ul id="uiListview" class="bui-listview">
          <li>
            <div class="bui-btn bui-box">
              <div class="span1">左滑动出现菜单</div>
              <i class="icon-listright"></i>
            </div>
          </li>
        </ul>

     - name: bui-listview-demo-custom
       displayName:   侧滑列表-dem自定义按钮
       display: true
       desc: 
       code: |
        // 侧滑列表自定义结构 js 初始化:
        var uiListview = bui.listview({
            id: "#uiListview",
            callback: function (e,menu) {
                //关闭菜单
                menu.close();
            }
        });
        // 侧滑列表自定义结构 html 对应的结构:
        <ul id="uiListview" class="bui-listview">
            <!-- 设置status属性可以避免重复渲染菜单 -->
            <li status="1">
                <div class="bui-btn bui-box">
                    <div class="span1">左右菜单</div>
                    <i class="icon-listright"></i>
                </div>
                <!-- data-moving 自定义左滑的距离 -->
                <div class="bui-listview-menu swipeleft" data-moving="240">
                    <div class="bui-btn primary">置顶</div>
                    <div class="bui-btn primary">修改</div>
                    <div class="bui-btn danger">删除</div>
                </div>
                <div class="bui-listview-menu swiperight">
                    <div class="bui-btn primary">修改</div>
                    <div class="bui-btn danger">删除</div>
                </div>
            </li>
        </ul>

     - name: bui-loading
       displayName:  页面正在加载
       display: false
       desc: 
       code: |
        var uiLoading = bui.loading($1);
            uiLoading.show();

     - name: bui-mask
       displayName:  页面遮罩初始化
       display: false
       desc: 
       code: |
        var uiMask = bui.mask($1);
            uiMask.show();

     - name: bui-number
       displayName:  数字加减条初始化
       display: false
       desc: 
       code: |
        var uiNumber = bui.number({
            id: '#uiNumber',
            value: 5,
            step: 2,
            min: 1,
            max: 10
        });

     - name: bui-number-demo
       displayName:  数字加减示例
       display: true
       desc: 
       code: |
        // 数字条 js 初始化:
        var uiNumber = bui.number({
          id: '#uiNumber',
          value: 5,
          min: 1,
          max: 10,
          render: false,
        })
        // 数字条 html 对应的结构:
        <div id="uiNumber" class="bui-number">
          <div class="bui-number-prev">
            <i class="icon-minus"></i>
          </div>
          <input type="text" value="2"/>
          <div class="bui-number-next">
            <i class="icon-plus"></i>
          </div>
        </div>

     - name: bui-pickerdate
       displayName:  日期控件初始化
       display: false
       desc: 
       code: |
        var pickerdateInput = \$("#pickerdateInput");
        var uiPickerdate = bui.pickerdate({
              handle:"#pickerdateInput",
              onChange: function(value) {
                pickerdateInput.val(value);
              }
            });

     - name: bui-pickerdate-demo
       displayName:  日期控示例
       display: true
       desc: 
       code: |
        // 日期控件 js 初始化:
        var input = $("#datepicker_input");
        var uiPickerdate = bui.pickerdate({
          handle:"#datepicker_input",
          onChange: function(value) {
            input.val(value);
          }
        });

        // 日期控件 html 对应的结构:
        <input type="text" id="datepicker_input" readonly>

     - name: bui-pullrefresh
       displayName:  下拉刷新初始化
       display: false
       desc: 
       code: |
        var uiPullrefresh = bui.pullrefresh({
            id: "#uiPullrefresh",
            onRefresh: onRefresh
        });
        //下拉的时候刷新数据
        function onRefresh () {
          var _self = this;
          // 执行往事件以后需要执行返回函数,第一次执行的时候,还没有返回对象,需要延迟执行
          _self.reverse();
        };

     - name: bui-pullrefresh-demo
       displayName:   下拉刷示例
       display: true
       desc: 
       code: |
        // 下拉刷新 js 初始化:
        var uiPullRefresh = bui.pullrefresh({
            id: "#uiPullrefresh",
            onRefresh: getData
        })
        //请求数据 或者其它操作
        function getData(){
            bui.ajax({
              url: "",
              data: {}
            }).then(function(res){

              //请求成功以后还原刷新前状态
              uiPullRefresh.reverse();
            },function(res){

              //请求失败以后还原刷新前状态
              uiPullRefresh.reverse();
            })
        }
        // 下拉刷新 html 对应的结构:
        <div id="uiPullrefresh" class="bui-scroll">
            <div class="bui-scroll-head"></div>
            <div class="bui-scroll-main">
                <ul class="bui-list">
                </ul>
            </div>
        </div>

     - name: bui-rating
       displayName:  星级评分初始化
       display: false
       desc: 
       code: |
        var uiRating = bui.rating({
            id: "#uiRating",
            value: 3
        });

     - name: bui-rating-demo
       displayName:   星级评示例
       display: true
       desc: 
       code: |
        // 评分星星 js 初始化:
        var uiRating = bui.rating({
            id: "#uiRating",
            value: 3
        });
        // 评分星星 html 对应的结构:
        <div id="uiRating" class="bui-rating"></div>

     - name: bui-scroll
       displayName:  列表滚动加载初始化
       display: false
       desc: 
       code: |
        var uiScroll = bui.scroll({
            id: "#${1:uiScroll}",
            page:1,
            pageSize:10,
            //children: ".bui-list li",
            onRefresh: onRefreshData,
            onLoad: onLoadData
        });
        // 下拉刷新
        function onRefreshData(){
          $2
        };
        // 上拉加载
        function onLoadData(){
          $3
        };

     - name: bui-scroll-demo
       displayName:   列表滚动加示例
       display: true
       desc: 
       code: |
        // 列表滚动加载 js 初始化:
        var uiScroll = bui.scroll({
            id: "#uiScroll",
            children: ".bui-list",
            page:1,
            pageSize:5,
            onRefresh: refresh,
            onLoad: getData
        })
        //刷新数据
        function refresh () {
            var page = 1;
            var pagesize = 5;
            getData(page,pagesize,"html");
        }
        //新增下一页数据
        function getData(page,pagesize,command){
          var command = command || "append";

          // 更新分页信息,如果高度不足会自动请求下一页
          uiScroll.updateCache(page,res.data);

          // 刷新的时候返回位置
          uiScroll.reverse();
        }

        // 列表滚动加载 html 对应的结构:
        <div id="uiScroll" class="bui-scroll">
            <div class="bui-scroll-head"></div>
            <div class="bui-scroll-main">
                <ul class="bui-list">
                </ul>
            </div>
            <div class="bui-scroll-foot"></div>
        </div>

     - name: bui-searchbar
       displayName:  搜索条初始化
       display: false
       desc: 
       code: |
        var uiSearchbar = bui.searchbar({
            id:"#${1:uiSearchbar}",
            callback: function (ui,keyword) {
              // 点击搜索
            },
            onInput: function(ui,keyword) {
              // 输入实时搜索
            },
            onRemove: function(ui,keyword) {
              // 删除关键词需要做什么其它处理
            }
        });

     - name: bui-searchbar-demo
       displayName:   搜索示例
       display: true
       desc: 
       code: |
        // 搜索条 js 初始化:
        // 计算列表的高度
        var listHeight = \$(window).height() - \$("header").height() - \$("#uiSearchbar").height();
        // 搜索结果的列表初始化
        var uiList = bui.list({
              id: "#uiScroll",
              url: "userlist.json",
              data: {},
              field: {
                data:"data"
              },
              page:1,
              pageSize:10,
              height: listHeight,
              template: template
          });
        // 搜索条的初始化
        var uiSearchbar = bui.searchbar({
          id:"#uiSearchbar",
          onInput: function(ui,keyword) {
            //实时搜索
          },
          onRemove: function(ui,keyword) {
            //删除关键词需要做什么其它处理
          },
          callback: function (ui,keyword) {

              //点击搜索
              \$("#uiScroll .bui-list").empty();

              // 重新初始化数据
              uiList.init({
                page: 1,
                data: {
                  "keyword":keyword
                }
              });
           }
        });
        // 列表生成模板
        function template (data) {
            var html = "";

            \$.each(data,function(index, el) {

                html += '<li class="bui-btn"><i class="icon-facefill"></i>'+el.name+'</li>';
            });

            return html;
        }
        // 搜索条 html 对应的结构:
        <div id="uiSearchbar" class="bui-searchbar bui-box">
                <div class="span1">
              <div class="bui-input">
                <i class="icon-search"></i>
                <input type="search" value="" placeholder="请输入关键字"/>
                    </div>
                </div>
                <div class="btn-search">搜索</div>
        </div>
        // 列表控件结构
        <div id="uiScroll" class="bui-scroll">
          <div class="bui-scroll-head"></div>
          <div class="bui-scroll-main">
            <ul class="bui-list">
            </ul>
          </div>
          <div class="bui-scroll-foot"></div>
        </div>


     - name: bui-select
       displayName:  选择列表初始化
       display: false
       desc: 
       code: |
        var uiSelect = bui.select({
            id:"#${1:uiSelect}",
            type:"${2:checkbox}",
            popup: ${3:false},
            data: [{
              "name":"广东",
              "value":"11"
            }],
            onChange: function (module,index) {
              //改变的时候触发
            }
        });

     - name: bui-select-demo
       displayName:   选择列示例
       display: true
       desc: 
       code: |
        // 选择列表 js 初始化:
        var uiSelect = bui.select({
          trigger:"#uiSelect",
          title: "请选择区域",
          data: [{
            "name":"广东",
            "value":"11"
          },{"name":"广西",
            "value":"22"
          }],
          //如果需要点击再进行操作,增加按钮
          buttons: [{name:"重置",className:""},{name:"确定",className:"primary-reverse"}],
          callback: function (argument) {

            var index = $(this).parent().index();
            if( index == 0 ){
              uiSelect.selectNone();
            }else{
              uiSelect.hide();
            }
          }
        })
        // 选择列表 html 对应的结构:
        <div id="uiSelect" class="bui-select bui-box">
          <div class="span1"></div>
          <i class="icon-listright"></i>
        </div>

     - name: bui-sidebar
       displayName:  侧滑栏初始化
       display: false
       desc: 
       code: |
        var uiSidebar = bui.sidebar({
            id: "#${1:uiSidebar}",
            trigger : "#${2:menu}"
        });

     - name: bui-sidebar-demo
       displayName:   侧滑示例
       display: true
       desc: 
       code: |
        // 侧边栏 js 初始化:
        var uiSidebar = bui.sidebar({
            id: "#uiSidebar"
        })

        // 侧边栏 html 对应的结构:
        <div id="uiSidebar" class="bui-sidebar-wrap">
          <div class="bui-sidebar swiperight">
            侧滑菜单
          </div>
          <div class="bui-page">
            页面正文
          </div>
        </div>

     - name: bui-slide
       displayName:  焦点图初始化
       display: false
       desc: 
       code: |
        var uiSlide = bui.slide({
            id:"#${1:uiSlide}",
            height:${2:200},
            zoom: true
        })

     - name: bui-slide-demo
       displayName:   焦点示例
       display: true
       desc: 
       code: |
        // 焦点图 js 初始化:
        var uiSlide = bui.slide({
            id:"#uiSlide",
            height:200,
            zoom: true
        })

        // 焦点图 html 对应的结构:
        <div id="uiSlide" class="bui-slide">
          <div class="bui-slide-main">
            <ul>
              <!-- 有多少个li则有多少个滑动 -->
              <li>
                <img src="slideshow.png" alt="">
                <div class="bui-slide-title">
                  标题
                </div>
              </li>
            </ul>
          </div>
          <div class="bui-slide-head">
            <ul>
              <li>1</li>
            </ul>
          </div>
        </div>

     - name: bui-slide-tab
       displayName:  选项卡初始化
       display: false
       desc: 
       code: |
        var uiSlideTab = bui.slide({
            id:"#${1:uiSlideTab}",
            menu:".bui-nav",
            children:".bui-tab-main > ul",
            scroll: true
        })

     - name: bui-slide-tab-demo
       displayName:   选项卡-示例
       display: true
       desc: 
       code: |
        // 选项卡 js 初始化:
        var uiTab = bui.slide({
            id:"#uiTab",
            menu:".bui-nav",
            children:".bui-tab-main > ul",
            scroll: true
        })

        // 选项卡 html 对应的结构:
        <div id="uiTab" class="bui-tab">
          <div class="bui-tab-head">
            <ul class="bui-nav">
              <li class="bui-btn active">Tab1</li>
              <li class="bui-btn ">Tab2</li>
            </ul>
          </div>
          <div class="bui-tab-main">
            <ul>
              <li>
                Tab1内容
              </li>
              <li>
                Tab2内容
              </li>
            </ul>
          </div>
        </div>

     - name: bui-slide-tab-pic-demo
       displayName:   多焦点图-示例
       display: true
       desc: 
       code: |
        // 焦点多图 js 初始化:
        var uiSlidePic = bui.slide({
            id:"#uiSlidePic",
            height:120
        })
                        
        // 焦点多图 html 对应的结构:
        <div id="uiSlidePic" class="bui-slide">
          <div class="bui-slide-main">
            <ul>
              <!-- 有多少个li则有多少个滑动 -->
              <li>
                <div class="bui-box-space">
                    <div class="span1">
                        <div style="background: yellow;height: 120px;"></div>
                    </div>
                    <div class="span1">
                        <div style="background: orange;height: 120px;"></div>
                    </div>
                    <div class="span1">
                        <div style="background: red;height: 120px;"></div>
                    </div>
                </div>
              </li>
            </ul>
          </div>
          <div class="bui-slide-head">
            <ul>
              <li>1</li>
            </ul>
          </div>
        </div>  

     - name: bui-slide-tab-scroll-demo
       displayName:   选项卡菜单滚动-示例
       display: true
       desc: 
       code: |
        // 选项卡 js 初始化:
        var tabScroll = bui.slide({
            id:"#tabScroll",
            menu:"#nav",
            children:".bui-tab-main > ul",
            scroll: true
        })    

        // 让顶部导航滚动到可视位置
        tabScroll.on("to",function (index) {

            var left = $("#nav li")[index].offsetLeft;
            document.getElementById("uiSlideNavbar").scrollLeft = left;
        })

        // 选项卡 html 对应的结构:
        <div id="uiSlideNavbar" class="bui-navbar">
          <ul id="nav" class="bui-nav">
            <li class="bui-btn active">Tab1</li><li class="bui-btn ">Tab2</li><li class="bui-btn">Tab3</li><li class="bui-btn ">Tab4</li><li class="bui-btn">Tab5</li><li class="bui-btn ">Tab6</li><li class="bui-btn">Tab7</li><li class="bui-btn ">Tab8</li>
          </ul>
        </div>
        <div id="tabScroll" class="bui-tab">
          <div class="bui-tab-main">
            <ul>
              <li>
                滑动的内容
              </li>
              <li>
                滑动的内容
              </li>
            </ul>
          </div>
        </div>

     - name: bui-slide-tab-head-demo
       displayName:   选项在头部示例
       display: true
       desc: 
       code: |
        //按钮在tab外层,需要传id
        var tabHead = bui.slide({
           id:"#tabHead",
           menu:"#tabHeadNav",
           children:".bui-tab-main > ul",
           scroll: true
        })
        <!-- 替换对应的main标签内容及header标签内容 -->
        <header class="bui-bar">
           <div class="bui-bar-left">
               <a class="bui-btn"><i class="icon-back"></i></a>
           </div>
           <div class="bui-bar-main">
               <ul id="tabHeadNav" class="bui-nav" style="width: 60%;">
                   <li class="bui-btn active">Tab1</li>
                   <li class="bui-btn ">Tab2</li>
               </ul>
           </div>
           <div class="bui-bar-right"></div>
        </header>
        <main>
           <div id="tabHead" class="bui-tab">
             <div class="bui-tab-main">
               <ul>
                 <li>
                   Tab1内容
                 </li>
                 <li>
                   Tab2内容
                 </li>
               </ul>
             </div>
           </div>
        </main>

     - name: bui-slide-tab-foot-demo
       displayName:   选项在底部示例
       display: true
       desc: 
       code: |
        //按钮在tab外层,需要传id
        var tabFoot = bui.slide({
           id:"#tabFoot",
           menu:"#tabFootNav",
           children:".bui-tab-main > ul",
           scroll: true
        })
        <!-- 替换对应的main标签内容及footer标签内容 -->
        <main>
           <div id="tabFoot" class="bui-tab">
             <div class="bui-tab-main">
               <ul>
                 <li>
                   Tab1内容
                 </li>
                 <li>
                   Tab2内容
                 </li>
               </ul>
             </div>
           </div>
        </main>
        <footer>
            <ul id="tabFootNav" class="bui-nav">
                <li class="bui-btn bui-box-vertical active"><i class="icon-home"><span class="bui-badges">15</span></i><div class="span1">Tab1</div></li>
                <li class="bui-btn bui-box-vertical"><i class="icon-share"></i><div class="span1">Tab2</div></li>
            </ul>
        </footer>

     - name: bui-slide-tab-side-demo
       displayName:   选项在边栏示例
       display: true
       desc: 
       code: |
        var tabWidth = $(window).width() - $("#tabNav").width();
        //按钮在tab外层,需要传id
        var tabSide = bui.slide({
            id:"#tabSide",
            menu:"#tabSideNav",
            children:".bui-tab-main > ul",
            width: tabWidth,
            scroll: true,
            animate: false,
            delay: true
        })
        // html 结构
        <style>
          #tabSideNav {
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            background: #fff;
            z-index: 10;
            width:2rem;
          }
          #tabSide {
            margin-left: 2rem;
          }
        </style>
        <ul id="tabSideNav" class="bui-list">
          <li class="bui-btn active">Tab1</li>
          <li class="bui-btn ">Tab2</li>
        </ul>
        <div id="tabSide" class="bui-tab">
          <div class="bui-tab-main">
            <ul>
              <li>Tab1内容</li>
              <li>Tab2内容</li>
            </ul>
          </div>
        </div>

     - name: bui-stepbar
       displayName:  步骤条初始化
       display: false
       desc: 
       code: |
        var uiStepbar = bui.stepbar({
          id: "#${1:uiStepbar}",
          data: [{
            title: "预立项申请",
            subtitle: "2016-04-2 10:30",
            content: "成功创建申请,已经提交至XX部门经理审批,请耐心等待"
          },{
            title: "立项审批",
            subtitle: "2016-04-21 10:30",
            content: "审批完成,资料递交中"
          }]
        });

     - name: bui-stepbar-demo
       displayName:   步骤示例
       display: true
       desc: 
       code: |
        // 步骤条 js 初始化:
        var uiStepbar = bui.stepbar({
          id: "#uiStepbar",
          data: [{
            title: "预立项申请",
            subtitle: "2016-04-2 10:30",
            content: "成功创建申请,已经提交至XX部门经理审批,请耐心等待"
          },{
            title: "立项审批",
            subtitle: "2016-04-21 10:30",
            content: "审批完成,资料递交中"
          }]
        });
        //激活第2步
        uiStepbar.value(1);
        // 步骤条 html 对应的结构:
        <div id="uiStepbar" class="bui-stepbar"></div>

     - name: bui-swipe
       displayName:  抽屉滑动控件初始化
       display: false
       desc: 
       code: |
        var uiSwipe = bui.swipe({
            id: "#uiSwipe",
            handle: ".bui-page",
            movingDistance: 200,
            direction: "xy",
        });

     - name: bui-swipe-demo
       displayName:   抽屉滑动控示例
       display: true
       desc: 
       code: |
        // 抽屉滑动控件 js 初始化:
        var uiSwipe = bui.swipe({
            id: "#uiSwipe",
            handle: ".bui-page",
            movingDistance: 200,
            direction: "xy",
        });
        // 抽屉滑动控件 html 对应的结构:
        <div id="uiSwipe" class="wrap">
            <!-- 侧滑菜单 -->
            <div class="swipeleft" data-moving="200">
                右边菜单
            </div>
            <div class="swiperight" >
                左边菜单
            </div>
            <div class="swipeup" >
                底部菜单
            </div>
            <div class="swipedown">
                顶部菜单
            </div>
            <!-- 正文内容 -->
            <div id="page" class="bui-page">
                正文内容
            </div>
        </div>
   - name: 常用方法
     lang: javascript
     display: false
     snippets:

     - name: bui-refresh
       displayName:  页面刷新
       desc: 
       code: |
        bui.refresh();

     - name: bui-load
       displayName:  页面跳转传参
       desc: 
       code: |
        bui.load({url:"$1",param:${2:{\} }});


     - name: bui-getPageParams
       displayName:  页面接收参数
       desc: 
       code: |
        var getParams = bui.getPageParams();
            getParams.done(function(result){
                ${1:console.log(result);}
            })

     - name: bui-back
       displayName:  页面后退
       desc: 
       code: |
        bui.back($1);

     - name: bui-ajax
       displayName:  数据请求
       desc: 
       code: |
        bui.ajax({
            url: "${1:http://}",
            data: {$2},//接口请求的参数
            // 可选参数
            method: "GET"
        }).then(function(result){
            // 成功
        },function(result,status){
            // 失败 console.log(status)//"timeout"
        });


     - name: bui-ready
       displayName:   设备都准备完原生事件初始化
       desc: 
       code: |
        var uiReady = bui.ready(function(){
            // 原生事件初始化
        });


     - name: bui-checkVersion
       displayName:  检查新版
       desc: 
       code: |
        bui.checkVersion({
            id: "#checkVersion",
            url: "versionUpdate.json",
            currentVersion: "",
            currentVersionCode: ""
        });

        // versionUpdate.json 示例
        /*
        {
            "versionName" : "1.4.1",
            "versionCode" : 20180118,
            "minVersionCode" : 20171010,
            "isForced" : false,
            "downloadUrl" : "http://www.easybui.com/downloads/source/bui/bui_latest.zip",
            "iosDownloadUrl" : "https://itunes.apple.com/cn/app/id1362470378?mt=8",
            "remark" : "<div style='text-align:left;'><p>1. 新增单页路由</p><p>2. 新增模块化开发</p><p>3. 支持路由切换动画</p><p>4. 修复已知bug</p></div>"
        }
        */

     - name: bui-animate
       displayName:  动画处理
       desc: 
       code: |
        /* 动画示例,支持链式动画,最后需要执行start(); */
        var uiAnimatePage = bui.animate(${1:"#page"});
            uiAnimatePage.left(${2:100}).start();

     - name: bui-toggle
       displayName:  动画切换
       desc: 
       code: |
        /* toggle动画示例 */
        var uiToggle = bui.toggle({id:"#page"});
            //在需要的地方使用显示隐藏效果,支持自定义动画
            //uiToggle.show();
            //uiToggle.hide();

     - name: bui-storage
       displayName:  数据存储
       desc: 
       code: |
        // 允许存储2条数据,默认1条
        var ${1:uiStorage} = bui.storage($2);
            // 保存第1条数据
            uiStorage.set("code","123");


     - name: bui-array-in
       displayName:   检测字符是否存在数返回索引
       desc: 
       code: |
        var arr = ["a","b"],
            index = bui.array.in("a", arr);

     - name: bui-array-index
       displayName:   检测字符是否存在数返回所有索引的数组
       desc: 
       code: |
        var arr = ["a","b","a"],
            index = bui.indexArray("a", arr);

     - name: bui-array-compare
       displayName:   检测字符是否存在数返回布尔值
       desc: 
       code: |
        var arr = ["a","b"],
            index = bui.array.compare(a, arr);

     - name: bui-array-excess
       displayName:   去除数组的重复返回数组
       desc: 
       code: |
        var arr = ["a","b","a","c"],
            newArr = bui.array.excess("a", arr);

     - name: bui-array-filter
       displayName:   过滤数返回符合条件的数组
       desc: 
       code: |
        var arr = ["a","b"],
            data = bui.array.filter("a",arr);

     - name: bui-array-remove
       displayName:   删除数组的某个返回剩下的数组
       desc: 
       code: |
        var arr = ["a","b"],
            data = bui.array.remove("a", arr);
   - name: 事件
     lang: javascript
     display: false
     snippets:

     - name: bui-on-onload
       displayName:  图片及文件资源初始完毕触发onload事件
       desc: 
       code: |
        bui.on("onload",function(){
            //图片及文件资源初始完毕
        })


     - name: bui-on-pageinit
       displayName:  DOM及BUI初始完毕触发pageinit事件
       desc: 
       code: |
        bui.on("pageinit",function(){
            //DOM及BUI初始完毕
        })

     - name: bui-on-pageready
       displayName:  原生事件初始完毕触发pageready事件
       desc: 
       code: |
        bui.on("pageready",function(){
            //DOM 及 BUI 及 原生方法 初始完毕
        })

     - name: bui-on-pagebefore
       displayName:  BUI初始完前触发pagebefore事件
       desc: 
       code: |
        bui.on("pagebefore",function(){
            //DOM初始完毕
        })

     - name: bui-router
       displayName:  单页路由
       desc: 
       code: |
          window.router = bui.router();

          bui.on("pageinit",function(){
              // 初始化路由
              router.init({
                  id: "#bui-router"
              })
          })

     - name: bui-router-load
       displayName:   单页路由跳转传参
       desc: 
       code: |
        router.load({url:"$1",param:{}});

     - name: bui-router-preload
       displayName:   单页路由预加载
       desc: 
       code: |
        router.preload([{url:"$1"}]).then(function(e){
          // 预加载成功以后执行
        });


     - name: bui-router-getPageParams
       displayName:   单页路由获取参数
       desc: 
       code: |
        var pageParams = router.getPageParams();

     - name: bui-router-loadPart
       displayName:   单页路由局部加载
       desc: 
       code: |
        router.loadPart({id:"$1",url:""});


     - name: bui-router-replace
       displayName:   单页路由页面替换
       desc: 
       code: |
        router.replace({url:"$1",param:{}});


     - name: bui-router-refresh
       displayName:   单页路由页面刷新
       desc: 
       code: |
        router.refresh();


     - name: bui-router-back
       displayName:   单页路由页面后退
       desc: 
       code: |
        router.back();


     - name: bui-loader-define
       displayName:  模块定义
       desc: 
       code: |
        loader.define(function(require,exports,module){

            var pageview = {};
            // 页面初始化定义
            pageview.init = function(){

            };
            // 初始化
            pageview.init();

            return pageview;
        })

     - name: bui-loader-require
       displayName:  模块加载
       desc: 
       code: |
        loader.require([],function(){
            // 加载某个模块
        })

     - name: bui-loader-import
       displayName:  模块资源引入
       desc: 
       code: |
        loader.import(["$1"],function(){
          // 创建成功以后执行回调
        });

     - name: bui-loader-map
       displayName:  模块配置
       desc: 
       code: |
        loader.map({
            moduleName: "$1",
            template: "",
            script: ""
        })


     - name: bui-loader-mapall
       displayName:  模块配置多个
       desc: 
       code: |
        loader.map({
            modules: {
                main: {
                    moduleName: "main",
                    template: "",
                    script: ""
                }
            }
        })
   - name: UI结构
     lang: html
     display: false
     snippets:

      - name: ui-html
        displayName:   页面依赖加载
        code: |
          <!DOCTYPE HTML>
          <html>
          <head>
              <meta http-equiv="Content-Type" content="text/html;charset=UTF-8" />
              <title>BUI</title>
              <meta name="format-detection" content="telephone=no" />
              <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no">
              <link rel="stylesheet" href="css/bui.css" />
          </head>
          <body>

              <!-- 依赖库 手机调试的js引用顺序如下 -->
              <script src="js/zepto.js"></script>
              <script src="js/plugins/fastclick.js"></script>
              <script src="js/bui.js"></script>
              <script>
                  bui.ready(function () {
                      // 这里写业务及控件初始化
                      $1
                  })
              </script>
          </body>
          </html>


      - name: ui-page
        displayName:   bui标准页面结构
        code: |
          <div class="bui-page">
              <header class="bui-bar">
                  <div class="bui-bar-left">
                      <!-- 左边按钮 -->
                      <a class="bui-btn" onclick="bui.back();"><i class="icon-back"></i></a>
                  </div>
                  <div class="bui-bar-main">页面标题</div>
                  <div class="bui-bar-right">
                      <!-- 右边按钮 -->
                  </div>
              </header>
              <main>
                  <!-- 正文内容 -->
              </main>
              <footer>
                  <!-- 底部内容 -->
              </footer>
          </div>

      - name: ui-accordion
        displayName:   折叠菜单结构
        code: |
          <dl id="uiAccordion" class="bui-accordion">
              <dt class="bui-btn bui-box">
                  <div class="span1">折叠菜单</div>
                  <i class="icon-accordion"></i>
              </dt>
              <dd>
                  折叠菜单内容
              </dd>
          </dl>

      - name: ui-router
        displayName:   单页路由结构
        code: |
          <!DOCTYPE HTML>
          <html lang="en-US">
          <head>
              <meta http-equiv="Content-Type" content="text/html;charset=UTF-8" />
              <title>BUI-单页模板</title>
              <meta name="format-detection" content="telephone=no" />
              <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no">
              <link rel="stylesheet" href="css/bui.css" />
          </head>
          <body>
              <div id="bui-router"></div>
              <script src="js/zepto.js"></script>
              <script src="js/plugins/fastclick.js"></script>
              <script src="js/bui.js"></script>
          </body>
          </html>

      - name: ui-list
        displayName:   下拉刷新列表结构
        code: |
          <div id="uiScroll" class="bui-scroll">
              <div class="bui-scroll-head"></div>
              <div class="bui-scroll-main">
                  <!-- 这里是滚动的内容区,如果不是ul,则需要修改对应的参数 -->
                  <ul class="bui-list">
                  </ul>
              </div>
              <div class="bui-scroll-foot"></div>
          </div>

      - name: ui-list-thumbnail
        displayName:   缩略图列表
        code: |
          <ul class="bui-list">
              <li class="bui-btn bui-box">
                  <div class="thumbnail"><img src="../images/applogo.png" alt=""></div>
                  <div class="span1">
                      <h3 class="item-title">标题</h3>
                      <p class="item-text">内容描述</p>
                  </div>
                  <i class="icon-listright"></i>
              </li>
          </ul>

      - name: ui-list-radio
        displayName:   单选列表
        code: |
          <ul class="bui-list">
              <li class="bui-btn bui-box bui-btn-line">
                  <div class="span1">
                      <label for="${1:choose}">单选</label>
                  </div>
                  <input id="${1:choose}" type="radio" class="bui-choose" name="interest" value="" text="" checked="checked">
              </li>
          </ul>

      - name: ui-list-icon
        displayName:   图标列表
        code: |
          <ul class="bui-list">
              <li class="bui-btn bui-box">
                  <div class="icon"><img src="../images/applogo.png" alt=""></div>
                  <div class="span1">文本</div>
                  <i class="icon-listright"></i>
              </li>
          </ul>

      - name: ui-list-arrow
        displayName:   箭头列表
        code: |
          <ul class="bui-list">
              <li class="bui-btn bui-box">
                  <div class="span1">文本</div>
                  <i class="icon-listright"></i>
              </li>
          </ul>

      - name: ui-list-checkbox
        displayName:   多选列表
        code: |
          <ul class="bui-list">
              <li class="bui-btn bui-box bui-btn-line">
                  <div class="span1">
                      <label for="${1:choose}">多选</label>
                  </div>
                  <input id="${1:choose}" type="checkbox" class="bui-choose" name="interest" value="" text="" checked="checked">
              </li>
          </ul>

      - name: ui-list-group
        displayName:   分组列表
        code: |
          <ul class="bui-list">
              <li class="bui-btn bui-btn-title">
                  分组标题
              </li>
              <li class="bui-btn bui-box">
                  <div class="span1">文本</div>
                  <i class="icon-listright"></i>
              </li>
          </ul>

      - name: ui-navbar
        displayName:   横向滚动导航
        code: |
          <div class="bui-navbar">
            <ul class="bui-nav">
              <!-- <li>之间不能有空隙 -->
              <li class="bui-btn">首页</li><li class="bui-btn">新闻</li>
            </ul>
          </div>

      - name: ui-nav
        displayName:   导航菜单结构
        code: |
          <ul class="bui-nav">
              <li class="bui-btn">文本</li>
          </ul>

      - name: ui-nav-icon
        displayName:   导航菜单图标结构
        code: |
          <ul class="bui-nav">
              <li class="bui-btn bui-box-vertical">
                  <i class="icon-home"></i>
                  <div class="span1">文本</div>
              </li>
          </ul>

      - name: ui-form
        displayName:   表单展示
        code: |
          <ul class="bui-list">
              <li class="bui-btn bui-box">
                  <label class="bui-label">帐号</label>
                  <div class="span1">
                      <div class="bui-value">username</div>
                  </div>
              </li>
          </ul>

      - name: ui-form-edit
        displayName:   表单编辑
        code: |
          <ul class="bui-list">
              <li class="bui-btn bui-box clearactive">
                  <label class="bui-label" for="${1:username}">帐号</label>
                  <div class="span1">
                      <input id="${1:username}" type="text" class="bui-input" value="" placeholder="请输入帐号">
                  </div>
              </li>
          </ul>

      - name: ui-form-btn
        displayName:   表单底部按钮
        code: |
          <div class="container-y">
              <div class="bui-box-space">
                  <div class="span1">
                      <div class="bui-btn round">重置</div>
                  </div>
                  <div class="span1">
                      <div class="bui-btn round primary">提交</div>
                  </div>
              </div>
          </div>

      - name: ui-radio
        displayName:   单选框
        code: |
          <input type="radio" class="bui-radio" name="$1" value="">

      - name: ui-choose
        displayName:   多选圆圈
        code: |
          <input type="${1:checkbox}" class="bui-choose" name="" value="">

      - name: ui-checkbox
        displayName:   多选框
        code: |
          <input type="checkbox" class="bui-checkbox" name="" value="">

      - name: ui-checkbox-custom
        displayName:   自定义多选框
        code: |
          <div class="bui-check">
              <input id="${1:uid}" type="radio" ><label for="${1:uid}">3:50元</label>
          </div>

      - name: ui-input
        displayName:   输入框
        code: |
          <input type="text" class="bui-input" value="" placeholder="请输入关键字">

      - name: ui-input-icon
        displayName:   图标输入框
        code: |
          <div class="bui-input">
              <i class="icon-search"></i>
              <input type="text" value="" placeholder="请输入关键字">
          </div>

      - name: ui-range
        displayName:   滑动条
        code: |
          <input type="range" class="bui-range" min="0" max="255" value="100">


      - name: ui-switch
        displayName:   切换按钮
        code: |
          <input type="checkbox" class="bui-switch" name="$1" value="">


      - name: ui-btn
        displayName:   按钮
        code: |
          <div class="bui-btn" href="$1">按钮</div>

      - name: ui-box
        displayName:   弹性布局
        code: |
          <div class="bui-box">
            <div class="span1">${1:<!--自适应内容-->}</div>
          </div>

      - name: ui-box-space
        displayName:   弹性布局有间隙
        code: |
          <div class="bui-box-space">
              <div class="span1">${1:<!--自适应内容-->}</div>
          </div>

      - name: ui-fluid
        displayName:    百分比布12等分
        code: |
          <div class="bui-fluid">
            <div class="span6">${1:内容}</div>
            <div class="span6">${1:内容}</div>
          </div>

      - name: ui-fluid-space
        displayName:    百分比布间隙
        code: |
          <div class="bui-fluid-space">
            <div class="span6">${1:内容}</div>
            <div class="span6">${1:内容}</div>
          </div>

      - name: ui-accordion
        displayName:   折叠菜单结构
        code: |
          <dl id="uiAccordion" class="bui-accordion">
              <dt class="bui-btn bui-box">
                  <div class="span1">折叠菜单</div>
                  <i class="icon-accordion"></i>
              </dt>
              <dd>
                  折叠菜单内容
              </dd>
          </dl>

      - name: ui-actionsheet
        displayName:   上拉菜单结构
        code: |
          <div id="${1:uiActionsheet}" class="bui-actionsheet container-x">
              <ul class="bui-list round">
                  <li class="bui-btn">示例菜单</li>
              </ul>
              <div class="bui-btn round">取消</div>
          </div>

      - name: ui-bar
        displayName:   横条结构
        code: |
          <div class="bui-bar">
              <div class="bui-bar-left">
                  <a class="bui-btn"><i class="icon-back"></i></a>
              </div>
              <div class="bui-bar-main">标题</div>
              <div class="bui-bar-right">
                  <a class="bui-btn"><i class="icon-menu"></i></a>
              </div>
          </div>

      - name: ui-bar-side
        displayName:    横左对齐结构
        code: |
          <div class="bui-bar bui-bar-side">
              <div class="bui-bar-left">
                  <a class="bui-btn"><i class="icon-back"></i></a>
              </div>
              <div class="bui-bar-main">标题</div>
              <div class="bui-bar-right">
                  <a class="bui-btn"><i class="icon-search"></i></a>
                  <a class="bui-btn"><i class="icon-menu"></i></a>
              </div>
          </div>

      - name: ui-header
        displayName:   header结构
        code: |
          <header class="bui-bar">
              <div class="bui-bar-left">
                  <a class="bui-btn"><i class="icon-back"></i></a>
              </div>
              <div class="bui-bar-main">标题</div>
              <div class="bui-bar-right">
                  <a class="bui-btn"><i class="icon-menu"></i></a>
              </div>
          </header>

      - name: ui-header-side
        displayName:    heade左对齐结构
        code: |
          <header class="bui-bar bui-bar-side">
              <div class="bui-bar-left">
                  <a class="bui-btn"><i class="icon-back"></i></a>
              </div>
              <div class="bui-bar-main">标题</div>
              <div class="bui-bar-right">
                  <a class="bui-btn"><i class="icon-search"></i></a>
                  <a class="bui-btn"><i class="icon-menu"></i></a>
              </div>
          </header>


      - name: ui-dialog
        displayName:   自定义对话框结构
        code: |
          <div id="${1:uiDialog}" class="bui-dialog">
              <div class="bui-dialog-head">对话框标题</div>
              <div class="bui-dialog-main">对话框内容</div>
              <div class="bui-dialog-close"><i class="icon-close"></i></div>
          </div>

      - name: ui-dropdown
        displayName:   下拉菜单结构
        code: |
          <div id="${1:uiDropdown}" class="bui-dropdown">
              <div class="bui-btn bui-box" >
                  <div class="span1">菜单名称</div>
                  <i class="icon-dropdown"></i>
              </div>
              <ul class="bui-list">
                  <li class="bui-btn bui-box" value="">
                      <div class="span1">
                          下拉菜单1
                      </div>
                      <i class="icon-listright"></i>
                  </li>
              </ul>
          </div>

      - name: ui-listview
        displayName:   侧滑列表结构
        code: |
          <ul id="${1:uiListview}" class="bui-listview">
              <li>
                  <div class="bui-btn bui-box">
                      <div class="span1">左滑动出现菜单</div>
                      <i class="icon-listright"></i>
                  </div>
              </li>
          </ul>

      - name: ui-listview-custom
        displayName:    侧滑列表结自定义按钮
        code: |
          <ul id="${1:uiListview}" class="bui-listview">
              <li status="1">
                  <div class="bui-btn bui-box">
                      <div class="span1">手动渲染</div>
                      <i class="icon-listright"></i>
                  </div>
                  <div class="bui-listview-menu swipeleft" data-moving="200">
                      <div class="bui-btn btn-red">删除</div>
                  </div>
              </li>
          </ul>

      - name: ui-number
        displayName:   数字条结构
        code: |
          <div id="${1:uiNumber}" class="bui-number">
              <div class="bui-number-prev"><i class="icon-minus"></i></div>
              <input type="text" value=""/>
              <div class="bui-number-next"><i class="icon-plus"></i></div>
          </div>

      - name: ui-panel
        displayName:   面板结构
        code: |
          <div class="bui-panel">
              <div class="bui-panel-head">面板标题</div>
              <div class="bui-panel-main">面板内容</div>
              <div class="bui-panel-foot">面板底部</div>
          </div>

      - name: ui-pickerdate
        displayName:   日期控件结构
        code: |
          <input type="text" id="uiPickerdate" readonly>

      - name: ui-progress
        displayName:   进度条结构
        code: |
          <div class="bui-progress">
              <div class="bui-progress-cell" style="width:30%"></div>
          </div>

      - name: ui-scroll
        displayName:   滚动加载列表结构
        code: |
          <div id="uiScroll" class="bui-scroll">
              <div class="bui-scroll-head"></div>
              <div class="bui-scroll-main">
                  <ul class="bui-list"></ul>
              </div>
              <div class="bui-scroll-foot"></div>
          </div>

      - name: ui-searchbar
        displayName:   搜索条结构
        code: |
          <div id="${1:uiSearchbar}" class="bui-searchbar">
              <div class="bui-input">
                  <i class="icon-search"></i>
                  <input type="text" placeholder="请输入关键字搜索" value=""/>
              </div>
          </div>

      - name: ui-select
        displayName:   选择列表按钮结构
        code: |
          <div id="${1:uiSelect}" class="bui-select bui-box">
              <div class="span1">请选择</div>
              <i class="icon-listright"></i>
          </div>

      - name: ui-select-list
        displayName:   选择列表自定义结构
        code: |
          <ul id="${1:uiSelect}" class="bui-list">
              <li class="bui-btn bui-box bui-btn-line">
                  <div class="span1">
                      <label for="${1:choose}">多选</label>
                  </div>
                  <input id="${1:choose}" type="checkbox" class="bui-choose" name="interest" value="" text="" checked="checked">
              </li>
          </ul>


      - name: ui-sidebar
        displayName:   侧滑栏结构
        code: |
          <div id="uiSidebar" class="bui-sidebar-wrap">
            <div class="bui-sidebar swiperight">
              侧滑栏内容 swiperight 为滑动的方向,不同位置修改不同方向
            </div>
            <div class="bui-page">
              页面内容
            </div>
          </div>

      - name: ui-slide-tab
        displayName:   选项卡结构
        code: |
          <div id="${1:uiSlideTab}" class="bui-tab">
              <div class="bui-tab-head">
                  <ul class="bui-nav">
                      <!-- 分屏菜单 active 是激活的TAB样式 -->
                      <li class="bui-btn active">Tab1</li>
                  </ul>
              </div>
              <div class="bui-tab-main">
                  <ul>
                      <li>
                          第1屏
                      </li>
                  </ul>
              </div>
          </div>

      - name: ui-slide
        displayName:   焦点图结构
        code: |
          <div id="${1:uiSlide}" class="bui-slide">
              <div class="bui-slide-main">
                  <ul>
                      <li>
                          <!--第1屏-->
                          <img src="$2" alt="">
                          <div class="bui-slide-title">图片标题</div>
                      </li>
                  </ul>
              </div>
              <!-- 分屏菜单 -->
              <div class="bui-slide-head">
                  <ul >
                      <li>1</li>
                  </ul>
              </div>
          </div>


      - name: ui-stepbar
        displayName:   步骤条结构
        code: |
          <div id="${1:uiStepbar}" class="bui-stepbar"></div>

      - name: ui-swipe
        displayName:   抽屉滑动结构
        code: |
          <div id="uiSwipe" class="bui-swipe">
            <div class="swiperight">
              菜单在左边
            </div>
            <div class="swipeleft">
              菜单在右边
            </div>
            <div class="swipeup">
              菜单在上边
            </div>
            <div class="swipedown">
              菜单在下边
            </div>
            <div class="bui-page">
              页面正文
            </div>
          </div>

      - name: ui-table
        displayName:   表格结构
        code: |
          <table class="bui-table">
              <tr>
                  <td>1</td>
              </tr>
          </table>